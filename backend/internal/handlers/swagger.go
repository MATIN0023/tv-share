package handlers

import "watch-party/internal/models"

// Swagger schema aliases (referenced from handler godoc comments).
type (
	swaggerUser           = models.User
	swaggerRoom           = models.Room
	swaggerMessage        = models.Message
	swaggerVideoFeed      = models.VideoFeed
	swaggerWatchHistory   = models.WatchHistory
	swaggerScheduledVideo = models.ScheduledVideo
)
